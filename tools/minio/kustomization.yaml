apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - certificate.yaml
  - rolebinding.yaml

helmCharts:
  - name: minio
    repo: https://charts.min.io/
    releaseName: minio
    namespace: minio
    valuesFile: values.yaml

