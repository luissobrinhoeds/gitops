apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: argocd-env-secret
spec:
  refreshInterval: '10s'
  secretStoreRef:
    name: hashicorp-vault
    kind: ClusterSecretStore
  target:
    name: argocd-env-secret
  dataFrom:
    - extract:
        key: kv/ocp-bmt/argocd
